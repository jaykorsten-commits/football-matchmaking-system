$kind: http-request
name: Reserve Queue
method: POST
url: '{{baseUrl}}/queue/:queue_code/reserve'
order: 1000
headers:
  - key: Content-Type
    value: application/json
  - key: X-API-Key
    value: '{{apiKey}}'
    description: API key for authentication
pathVariables:
  - key: queue_code
    value: '{{queueCode}}'
    description: The queue code to reserve (e.g., eu_01)
body:
  type: json
  content: |-
    {
      "job_id": "job_abc123xyz"
    }
